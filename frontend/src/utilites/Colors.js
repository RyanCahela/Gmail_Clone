import { grey, blue } from "@mui/material/colors";

export const greyMedium = grey[600];
export const greyLight = grey[100];
export const blueLight = blue[100];

const colors = {
  greyMedium,
  greyLight,
  blueLight,
};

export default colors;
